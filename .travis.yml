os:
  - linux
  - osx
language: generic
sudo: required
dist: trusty
osx_image: xcode9.1
install:
  - eval "$(curl -sL https://swiftenv.fuller.li/install.sh)"
script:
  - swift build
notifications:
  email: false